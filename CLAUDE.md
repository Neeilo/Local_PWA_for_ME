# CLAUDE.md — Neil OS PWA

本檔案是 Claude Code 在這個專案工作時的行為契約。基於 Karpathy / Forrest Chang / Mnimiy 提出的 12 條原則，但依本專案實際踩過的坑做了取捨與具體化——不是通用範本照搬。
偏好：非瑣碎任務寧可謹慎、慢一點，也不要快但錯。瑣碎任務用你的判斷力，不用每件事都套完整流程。

## 專案速覽
- Stack：純 HTML/CSS/JS + Google Apps Script（Web App，唯一的「後端」）+ Google Sheet（唯一的「資料庫」）+ Service Worker + GitHub Pages 靜態託管
- 沒有傳統帳密系統，身份驗證靠 LINE `ALLOWED_USER_IDS` 白名單
- 單人維護，沒有 CI 強制檢查——這份檔案就是唯一的品質關卡
- Notion 是決策的 single source of truth，架構決策一律先有 ADR 才實作
- 目前已知未解衝突：`codex/security-sync-hardening-20260916` 分支（尚未 merge、未 rebase 到 ADR-009；兩者都動到 sync/離線邏輯）

---

## Rule 1 — 先想清楚，不要用猜的
遇到不確定的行為（尤其是「離線時該怎麼顯示」「這筆資料該不該算已刪除」這類已經改過一次決策的地方），停下來問，不要延續舊假設或自己決定。
若你發現有更簡單的做法，主動提出。

## Rule 2 — 簡單至上
只寫解決當下問題所需的最少程式碼。不要為了「以後可能會用到」加抽象層、加 library、加設定選項。
自問：資深工程師看到這段會不會覺得太複雜？會的話簡化。

## Rule 3 — 手術式修改
只動需求直接相關的範圍。改 sync 邏輯時不要順手重構其他模組；共用白板功能重用 tasks/expenses/notes 的欄位時，不要順手「順便優化」那幾個模組。只清理因這次修改而變成無用的東西，其他一律不動。

## Rule 4 — 目標導向，且定義可驗證的成功標準
不是「照步驟做完」，是「定義完成後怎麼確認它真的對」。
例如：「upsert 寫入後立刻用 GET 確認資料已更新」「soft-delete 的 `del` flag 要在所有讀取路徑都驗證過才算完成，不是只測列表頁」。

## Rule 5 — 只用在判斷力任務（低優先，目前關聯性低）
Neil OS 本身沒有 runtime AI 決策元件（分類、路由都是純邏輯）。這條先保留，若未來把 friday-ai-agent 的能力接進 Neil OS 才會真正用到；目前可視需要略過。

## Rule 6 — Token 預算不是參考用的
單一任務（例如一次 sync 邏輯修改）：預算內完成，接近上限就先總結進度，不要硬撐。
一次對話：接近預算就主動總結「已做/已驗證/剩下什麼」，建議重開對話或交給 grill-me 收斂，而不是默默繼續燒。

## Rule 7 — 衝突要攤開講，不要兩邊都照顧
**這條直接對應目前的真實狀況**：`codex/security-sync-hardening-20260916` 分支與已 merge 的 ADR-009 都改到 sync/離線邏輯。如果你同時看到兩套寫法，不准自己「融合」出一個兩邊都滿足的版本——那是最難維護的程式碼。
選一個（通常是較新、測試較完整的那個），說明理由，把另一個標記為待清理，交給我決定。

## Rule 8 — 寫之前先讀
改任何 sync / tombstone / archive 相關程式碼前，先讀完整條資料流：LINE bot → Apps Script → Sheet → 前端 fetch → localStorage。「看起來不相關」是這個專案最危險的一句話，尤其白板功能是靠共用 tag 欄位接到既有模組的，看起來獨立但其實共用底層資料。

## Rule 9 — 測試要驗證「為什麼」，不是只驗證「有沒有跑」
Sync 邏輯（upsert / tombstone / archive）的自動化測試要能反映業務邏輯，不能是寫死回傳值讓測試亮綠燈的假測試。
至少要涵蓋這些邊界案例：匯出中途失敗、連續快速寫入同一筆、離線時的行為（現在的決策是顯示連線錯誤，不是顯示快取）。

## Rule 10 — 每個重要步驟做完就要回報進度
完成一個階段性步驟後，主動總結「做了什麼／驗證了什麼／剩下什麼」，格式要能直接貼進 Notion（配合現有的 handoff 紀律：Claude.ai 先寫清楚才交給 Claude Code）。
如果你在執行過程中已經說不清楚現在的狀態，立刻停下來，不要憑印象繼續。

## Rule 11 — 配合現有慣例，即使你不同意
這是單人專案，風格一致性比個人品味重要。就算你覺得某個寫法有更好的替代方案，也不要默默用新風格取代——你可以提出來討論，但不要在程式碼裡「悄悄分岔」成兩種寫法並存。

## Rule 12 — 主動講錯，不要默默失敗
「archive 完成」如果有任何一筆資料匯出失敗卻被跳過，這句話就是錯的。「測試通過」如果你跳過了任何一個案例，這句話就是錯的。
特別是 Apps Script 有配額限制、archive 是兩階段流程——任何一個環節沒把握，明講，不要用「應該沒問題」帶過。

---

## 明確排除的「常見但有害」提示詞（依文章實測結論）
- 不用具體範例硬灌規則——範例會讓你過度擬合，變得不知變通；上面每條都用抽象原則表達
- 不用情緒喊話或角色扮演（「請仔細思考」「像資深工程師一樣」）——沒有實質作用，只會稀釋指令遵循度
- 不綁定特定工具（例如不規定「永遠用 ESLint」）——這專案沒有 lint 工具，工具鎖死的規則會默默失效

