# 交接：ADR-009 後的清理（PR「清理 ADR-009 留下的死碼」）

> **⚠️ 這個檔案是暫時的。下面的工作全部完成、code review 通過之後，合併之前要先刪掉它。**
> repo 整包都會發布到 GitHub Pages（`deploy.yml` 用的是 `path: .`），留著會被公開。

- **建立：** 2026-09-24，Claude（Cowork），Ryze 核對 ADR＋JHIN code review
- **基準：** `main@46367dd`（PR #28 合併後）
- **Notion：** Neil OS 專案頁「🧹 維護」待辦、📍 程式現況活頁第五節

---

## 一、這支 PR 已經做的（只有純刪除）

| 項目 | 位置 | 為什麼是死碼 |
|---|---|---|
| `dedupeById()`、`dedupeState()` | `index.html` | ADR-007 票 B 的前端去重。ADR-009 之後沒有任何呼叫者（pull 改成整張覆蓋） |
| `migrateReviewKeys()` | `index.html`，`migrate()` 改成 `let changed=false;` | `pullFromCloud()` 和 `applyPendingOverlay()` 都用 `reviewKey()` 建鍵，純日期舊鍵已不可能出現 |
| SW 版號 | `sw.js` v21 → v22 | 動到 `index.html` 的慣例 |

驗證：`npm test` 201/201、`npm run scan` 通過。行為沒有改變（被刪的函式沒人呼叫）。

---

## 二、剩下要做的（依序；同一支 PR 裡做完）

### 1. 🔴 P1：選完身份後不要重送整份雲端資料

**位置：** `index.html` 的 `pickIdentity()`

**問題：** 目前是 `syncFromCloud().then(pushAllRows);`。pull 完之後 state 就是雲端快照，`pushAllRows()` 會把**每一筆**逐筆重新 upsert。佇列一次只送一筆，幾百筆要送好幾分鐘；這段時間家人在另一支手機改的東西，會被這份舊快照蓋回去。這等於 ADR-009 §一.2 要消滅的 DATA-01，換了個速度又回來。
JHIN 已經用測試實證：雲端有 20 筆任務，選完身份就重送 20 筆。

**為什麼可以直接拿掉：** `queueUpsert()` 在還沒有身份時就會把寫入**排進待送佇列**（只是 `flushOutbox()` 因為沒有身份而先不送）。身份確定前寫的東西本來就在佇列裡，只要把佇列送出去就好。

**改法：**
```js
  // 身份定了才送得出去。身份確定前寫的東西早就排在待送佇列裡（queueUpsert
  // 沒有身份時只排不送），這裡只要把佇列送出去就好。
  // ⚠️ 不要重送 state：pull 之後 state 就是雲端快照，逐筆重送要好幾分鐘，
  // 那段時間家人在別支手機改的東西會被這份舊快照蓋回去（DATA-01 換個速度回來）。
  syncFromCloud().then(flushOutbox);
```
然後刪掉 `pushAllRows()` 和它上方的註解（唯一呼叫者就是這裡）。

### 2. 🟠 P4：後端 `replaceAll` 改成明確拒絕

**位置：** `apps-script/Code.gs` 的 `handlePwaSync_()`

**問題：** 前端已經不送 `replaceAll`（只送 `upsert`／`completeRecurring`／`archivePurge`），但後端分支還活著。它一個請求就能 `clearContents()` 整張表；而 `CLOUD_SECRET` 公開在部署出去的 `index.html` 裡、`doGet` 也讀得到 `line_users`，拿到網址的人就湊齊了條件。

**改法：**
- 把 `if (body.action === 'replaceAll') { … }` 整段換成：
```js
  /**
   * replaceAll 已退場（ADR-009 §一.2）：前端改送單筆 upsert 之後，沒有任何呼叫端。
   *
   * 不只是刪掉它，而是明確拒絕並記一筆 logs：它是整個系統破壞力最大的一個動作
   * （一個請求清空整張表），而 CLOUD_SECRET 公開在部署出去的 index.html 裡、
   * doGet 讀得到 line_users。留著它等於留一個「一次清空」的按鈕給拿到網址的人。
   * 萬一真有舊前端送上來，logs 裡看得到，不會安靜地什麼都沒發生。
   */
  if (body.action === 'replaceAll') {
    console.log('🚫 拒絕 replaceAll → ' + body.sheet + '：已於 ADR-009 退場');
    try {
      logTransaction_('同步', '失敗', 'replaceAll ' + body.sheet,
        '已退場的動作，拒絕執行', 'ADR-009 後前端只送 upsert；若這筆來自舊版 App，請重開 App', '', body.line_id || '');
    } catch (err) {
      console.log('寫 logs 失敗（主流程不受影響）：' + err);
    }
    return jsonOut({ error: 'action_retired', action: 'replaceAll' });
  }
```
  （不用 `logCleanup_()`：它把狀態寫死成「成功」。）
- 刪掉 `dedupeById_()` 和它的區塊標題與註解（唯一呼叫者是舊的 `replaceAll` 分支；註解裡提到的前端 `dedupeState()` 也已經被這支 PR 刪掉）
- 刪掉 `summarizeIds_()`（同上，唯一呼叫者是舊分支）
- ⚠️ **`NO_REPLACE_ALL` 不能刪**：`archivePurge` 還靠它擋 `logs`／`line_users`。改名為 `NOT_FRONTEND_SHEETS`（規則不變），更新它的註解，並把 `NO_PWA_WRITE` 註解裡的「它比 NO_REPLACE_ALL 更嚴」一起改名

**順序安全性：** `NO_PWA_WRITE`（`_guide`）的檢查排在所有 action 之前，所以 `tests/sheet-guide.test.mjs` 對 `_guide` 送 `replaceAll` 仍然會拿到 `sheet_not_writable`，不受影響。

### 3. 🟢 過期註解（7 處，描述的是 ADR-009 之前的行為）

用搜尋字串定位（行號會變）：

| 檔案 | 搜尋 | 問題 → 改成 |
|---|---|---|
| `index.html` | `前端存檔是整包 replaceAll，一旦回推`（LOG 區塊的 HTML 註解） | → 「它是 Apps Script 單向寫入的記錄，前端只看不寫」 |
| `index.html` | `state 會被整包推上雲端，而身份選擇` | → 「它是『這支手機是誰在用』，本來就不該跨裝置一致」 |
| `index.html` | `雲端同步（Google Sheets，僅 tasks/reviews/moods/notes）` 與下方兩行「吞掉錯誤回空陣列／合併規則只補不刪」 | **跟緊接著的 ⚠️ 註解互相矛盾**。標題改成 `雲端讀取（tasks／reviews／moods／notes／expenses）`，刪掉那兩行 |
| `index.html` | `為什麼一定要這樣：前端存檔是整包 replaceAll。logs 只要進了 state` | → logs 的唯一寫入者是 Apps Script，進了 state 或佇列就可能拿舊的 upsert 回去蓋掉後端剛寫的記錄 |
| `index.html` | `不先補齊就編輯，下一次 save() 的 replaceAll` | → 背景時輪詢是停的（ADR-009 §二），回前景第一眼要是最新的，不必等下一輪 15 秒 |
| `index.html` | `身份定了才推得上去。先 pull 補齊再 push` | 隨第 1 項一起改 |
| `apps-script/Code.gs` | `⚠️ 這一段目前沒有任何呼叫端，是刻意的` | 2026-09-16 起已全數接上線；改寫成「已接線，Phase 0 時先寫函式與測試是 ADR-009 要求的施工順序」，保留 `ensureAdr009Columns()` 是手動安裝函式那一段 |

### 4. 🟢 文件
- `README.md` 第 3 行「資料主要存在本機（localStorage），並會同步備份到 Google Sheets」→ 資料存在 Google Sheets（雲端是唯一真相，ADR-009）；本機只放待送佇列與個人設定
- `package.json` 的 `description`「本地優先 PWA」→「雲端為真相的 PWA（Google Sheets）」

### 5. 🧪 測試：新增 `tests/cleanup-review.test.mjs`

**先寫、先在未修改的程式上跑，確認前兩支是紅燈**（第三支是守護測試，本來就該綠），再做第 1、2 項。JHIN 已經實跑確認過：舊程式上 P1 那支會得到 `actual: 20`。

```js
/**
 * 清理小票的回歸測試（2026-09-24 JHIN code review）
 *
 * 三件事：
 *   1. 選身份之後，只送「身份確定前就排好」的那幾筆，不把整份雲端快照重送一遍
 *   2. replaceAll 已退場：後端收到要明確拒絕，整張表一個字都不能動
 *   3. logs／line_users 不歸前端管的保護，在 replaceAll 退場後仍然擋得住 archivePurge
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { loadFrontend } from './fake-browser.mjs';
import { loadCodeGs, FakeSheet } from './fake-apps-script.mjs';

const ME = 'Uneil';
const SECRET = 'test-cloud-secret';

describe('選身份之後的補推', () => {
  function cloudWith20Tasks() {
    const tasks = Array.from({ length: 20 }, (_, i) => ({
      id: String(1000 + i), text: '雲端任務 ' + i, is_completed: '', created_at: '2026-09-01T00:00:00Z',
      priority: 'M', line_id: 'Ufamily'
    }));
    return ({ url, body }) => {
      if (!body && url.includes('sheet=tasks')) return { json: () => Promise.resolve({ data: tasks }) };
      if (!body && url.includes('sheet=line_users')) {
        return { json: () => Promise.resolve({ data: [{ line_id: ME, display_name: 'Neil', is_active: 'TRUE' }] }) };
      }
      return null;
    };
  }
  async function settle(e, rounds = 400) {
    for (let i = 0; i < rounds; i++) await new Promise((r) => setImmediate(r));
  }

  test('只送身份確定前排好的那一筆，不重送雲端上既有的 20 筆', async () => {
    const e = loadFrontend({ fetchImpl: cloudWith20Tasks() });
    e.raw('myLineId = ""');
    e.raw('localOnly = false');
    e.raw('state = EMPTY_STATE()');
    e.raw('outbox = []');
    e.call('queueUpsert', 'notes', { id: '77', text: '選身份前記的', line_id: '' });
    assert.equal(e.call('pendingCount'), 1, '前提：身份未定時寫入只排不送');

    // 畫面相關的部分與這裡要驗的無關（假 DOM 也撐不起導覽列重排），換成空函式
    e.raw('applyIdentity = function(){}; closeIdentityGate = function(){}');
    e.raw('gateOptions = [{ line_id: "' + ME + '", display_name: "Neil" }]');
    e.call('pickIdentity', 0);
    await settle(e);

    const writes = e.calls.filter((c) => c.body && c.body.action);
    assert.equal(writes.filter((c) => c.body.sheet === 'tasks').length, 0,
      '雲端既有的任務不該被重送——那是別人的最新版本，重送等於拿快照蓋回去');
    assert.equal(writes.filter((c) => c.body.sheet === 'notes' && c.body.record.id === '77').length, 1,
      '身份確定前記的那一筆要送出去');
    assert.equal(e.call('pendingCount'), 0, '佇列要清空');
  });
});

describe('replaceAll 已退場（ADR-009 §一.2）', () => {
  function env() {
    return loadCodeGs({
      properties: { CLOUD_SECRET: SECRET },
      sheets: {
        tasks: new FakeSheet('tasks', [['id', 'text', 'line_id'], ['1', '買菜', ME], ['2', '繳費', ME]]),
        logs: new FakeSheet('logs', [['id', 'ts', 'source', 'status', 'input', 'result', 'detail', 'target_row', 'user_id']]),
        line_users: new FakeSheet('line_users', [['line_id', 'display_name', 'is_active', 'is_admin'], [ME, 'Neil', 'TRUE', 'TRUE']])
      }
    });
  }
  function post(e, body) {
    const req = { postData: { contents: JSON.stringify(Object.assign({ secret: SECRET, line_id: ME }, body)) } };
    return JSON.parse(e.call('handlePwaSync_', req).body);
  }

  test('對 tasks 送 replaceAll：被拒絕，整張表原封不動', () => {
    const e = env();
    const before = JSON.stringify(e.ss.getSheetByName('tasks').values);
    const out = post(e, { sheet: 'tasks', action: 'replaceAll', headers: ['id', 'text', 'line_id'], records: [] });
    assert.ok(out.error, '要回錯誤，不能回 success');
    assert.notEqual(out.success, true);
    assert.equal(JSON.stringify(e.ss.getSheetByName('tasks').values), before,
      '一個請求就清空整張表，是這個系統破壞力最大的一個動作——它不該還能被觸發');
  });

  test('logs 與 line_users 仍然不能被 archivePurge 動到', () => {
    const e = env();
    for (const sheet of ['logs', 'line_users']) {
      const before = JSON.stringify(e.ss.getSheetByName(sheet).values);
      const out = post(e, { sheet, action: 'archivePurge', key_field: 'id', keys: ['1'] });
      assert.equal(out.error, 'sheet_not_purgeable', sheet + ' 要被擋下');
      assert.equal(JSON.stringify(e.ss.getSheetByName(sheet).values), before);
    }
  });
});
```

---

## 三、驗收標準

- [ ] 新測試：修改前兩支紅燈、修改後全綠（**要確定跑到的是新測試**，見 09-24 執行日誌的 `git stash` 教訓）
- [ ] `npm test` 全綠（預期 204 項）、`npm run scan` 通過
- [ ] `grep -n "pushAllRows\|dedupeById_\|summarizeIds_\|NO_REPLACE_ALL" index.html apps-script/*.gs` 只剩改名註記那一行
- [ ] SW 版號**不用再升**：這支 PR 已經是 v22，同一支 PR 內不重複遞增
- [ ] **刪除本檔 `交接.md`**，再請 JHIN（code review）看完整 diff
- [ ] 合併後：Neil 在 LINE 傳任一無效前綴，確認後端已部署（版本指紋）；更新 📍 程式現況活頁第五節

## 四、刻意不在這支 PR 做的

- `personal-os-state-v1` 只寫不讀、今日指南針被輪詢清空的 bug、`init()` 裡「localStorage 那條路從頭到尾都沒被碰過」這句註解 → 牽涉離線逃生門的產品行為，交給 grill-me ⑨
- `archive` 欄：`ensureAdr009Columns()` 會建立但沒有程式讀寫 → 等 Neil 決定
- **`CLAUDE.md` 有兩處過時**（「身份驗證靠 LINE `ALLOWED_USER_IDS` 白名單」、「沒有 CI 強制檢查」）→ 那是 Neil 的行為契約，依 Rule 11 不擅改，由 Neil 決定
- `codex/security-sync-hardening-20260916` 分支 → 依 CLAUDE.md Rule 7，不碰
